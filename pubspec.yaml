name: macos_proxy_manager
description: Your Mac's MenuBar Proxy Control
repository: https://github.com/ZhiaDev/mac-proxy-manager

platforms:
  macos:

version: 1.1.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  flutter_svg: ^2.0.7
  google_fonts: ^6.1.0
  audioplayers: ^5.2.0
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true

  assets:
    - assets/